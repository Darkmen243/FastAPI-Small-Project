# Simple Example FastApi project for improving my skills with it.

So far I have a User model within DB and a couple endpoints connected with it, like creation of a user and getting a single user or all of them

# Update#1:

Added Orders and Items models and CRUD Operations, converted database queries to async
